// program to delete the last digit.
#include <stdio.h>

int main()
{
  int number;
  printf("Enter a number: ");
  scanf("%d", &number);
  printf("The remaining no. is: %d\n", number/10);
  return 0;
}
